Sources for the plugin `plugin.texturePackerHelpers`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.texturePackerHelpers" = {
            publisherId = "com.roaminggamer",
        },
    },
}
```
